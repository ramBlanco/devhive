# Inclusive Language

Always use professional and inclusive terminology in your code, comments, documentation, and agent responses:

1. Use **allowlist / blocklist** instead of whitelist / blacklist.
2. Use **main / primary** branch instead of master branch.
3. Use **primary / replica** or **leader / follower** instead of master / slave architectures.